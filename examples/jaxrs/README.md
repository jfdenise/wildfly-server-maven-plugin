# JAX-RS WildFly server plugin example

Build aserver containing a JAX-RS resource.

Build and run
=============

* To build: `mvn package`
* To run: `./target/server/standalone.sh`
* Access the application: `http://127.0.0.1:8080/hello`
